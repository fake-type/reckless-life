import { createThemeContract } from '@vanilla-extract/css'

const themesContract = createThemeContract({
  fonts: {
    body: ''
  },

  backgroundColor: { main: '' }
})

export default themesContract
